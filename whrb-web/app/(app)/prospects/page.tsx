import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function ProspectsPage() {
  return (
    <PagePlaceholder
      eyebrow="Pipeline"
      title="All Prospects"
      description="Master browseable list with search, tier filter, and priority-score sorting. Arrives in Stage 6 alongside the shared data grid."
      stage="Stage 6"
    />
  );
}

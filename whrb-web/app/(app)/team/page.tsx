import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function TeamPage() {
  return (
    <PagePlaceholder
      eyebrow="Directory"
      title="Team"
      description="Roster of sales reps with assignment counts, activity, and contactability. Lands in Stage 6 once the shared data grid is in place."
      stage="Stage 6"
    />
  );
}

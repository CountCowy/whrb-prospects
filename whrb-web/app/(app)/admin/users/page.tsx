import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function AdminUsersPage() {
  return (
    <PagePlaceholder
      eyebrow="Admin"
      title="Users & access"
      description="Invite reps, assign roles, and revoke access. Ships in Stage 9 alongside the admin shell."
      stage="Stage 9"
    />
  );
}

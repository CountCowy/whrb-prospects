import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function AdminFeedbackPage() {
  return (
    <PagePlaceholder
      eyebrow="Admin"
      title="Feedback queue"
      description="Triage rep-submitted data corrections and prospect-quality flags back into the scoring model. Ships in Stage 9."
      stage="Stage 9"
    />
  );
}

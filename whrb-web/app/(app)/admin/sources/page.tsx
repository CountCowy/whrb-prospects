import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function AdminSourcesPage() {
  return (
    <PagePlaceholder
      eyebrow="Admin"
      title="Sources"
      description="Configure Spinitron, intake forms, and external feeds that hydrate the prospect pipeline. Ships in Stage 9."
      stage="Stage 9"
    />
  );
}

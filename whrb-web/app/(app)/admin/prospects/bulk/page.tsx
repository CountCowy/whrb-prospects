import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function AdminBulkPage() {
  return (
    <PagePlaceholder
      eyebrow="Admin"
      title="Bulk prospect actions"
      description="Multi-select reassignment, tier overrides, and bulk state changes with a one-step undo. Ships in Stage 10b."
      stage="Stage 10b"
    />
  );
}

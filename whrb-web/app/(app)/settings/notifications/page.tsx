import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function NotificationsPreferencesPage() {
  return (
    <PagePlaceholder
      eyebrow="Settings"
      title="Notification preferences"
      description="Per-user toggles for digest cadence, assignment pings, and handoff alerts. Ships in Stage 10b."
      stage="Stage 10b"
    />
  );
}

requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
playwright>=1.40
pdfplumber>=0.10
pandas>=2.1
rapidfuzz>=3.6
tenacity>=8.2
python-dotenv>=1.0
email-validator>=2.1
dnspython>=2.4
yelpapi>=2.5
tldextract>=5.1
httpx>=0.27
requests-cache>=1.2
supabase>=2.28
psycopg2-binary>=2.9
# Pinned (not >=) because the external-calendar-sync workflow installs
# these explicitly and they are not yet in requirements.lock. Bump
# intentionally and run pip-compile to regenerate the lockfile when a
# version change is needed; do not relax to >=.
icalendar==6.1.0
recurring-ical-events==3.4.0
